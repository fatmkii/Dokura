services:
  dokura:
    build:
      context: ../..
    image: dokura:stage-08
    environment:
      DOKURA_CONTENT_DIR: /data/content
      DOKURA_METADATA_DIR: /data/metadata
      DOKURA_CONFIG_DIR: /data/config
    ports:
      - "${DOKURA_STAGE8_PORT:-18008}:8000"
    init: true
    read_only: true
    tmpfs:
      - /tmp:size=128m,mode=1777
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    volumes:
      - ${DOKURA_STAGE8_DATA:?}/Content:/data/content:ro
      - ${DOKURA_STAGE8_DATA:?}/MetaData:/data/metadata
      - ${DOKURA_STAGE8_DATA:?}/Config:/data/config
    healthcheck:
      test: ["CMD", ".venv/bin/python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/v1/health', timeout=2)"]
      interval: 2s
      timeout: 2s
      retries: 20
      start_period: 2s
