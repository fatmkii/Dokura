# 阶段 5 验收报告

## 范围

- 会话内跨分页选择快照、30 分钟空闲失效、刷新恢复、取消/退出/操作完成释放。
- ZIP 永久删除、移动、重命名和单文件重新处理；文件夹新建、移动、重命名与空目录删除。
- 名称、UTF-8 路径长度、Windows 保留名、大小写冲突、真实路径、同盘与五秒互斥校验。
- 失败任务批量重试、重新扫描、任务分页与两秒轮询、APIkey 一次性轮换展示、密码修改和缓存清理。
- 当前及轮转日志的级别筛选、最终 1,000 条读取、最多十个文件和完整 ZIP 归档下载。

## 一键命令

```bash
scripts/acceptance/stage-05.sh
```

脚本使用 `mktemp` 创建独立 Content、MetaData 和 Config，容器以宿主 UID/GID 运行。Content 挂载为读写，但仅指向该临时目录；退出时停止容器并删除临时树，绝不挂载或操作仓库 `Content/` 样例。

## 自动断言

- 17 项阶段 5 管理领域测试覆盖危险名称、路径逃逸、符号链接目标、跨文件系统、身份竞态、忙碌超时、快照过期、用户重处理命令和非空目录。
- 批量移动两个同名 ZIP 时保留一个成功项，冲突项不覆盖并返回简短原因。
- 文件及目录移动保留后代 ZIP UUID、评分、封面缓存关联和活动任务路径；数据库提交失败会回滚文件系统重命名。
- `0–3` 星筛选结果以每页 25 项显示两页，服务端快照选择全部 30 项，经永久删除确认后全部从筛选结果消失。
- 删除预览与执行之间统计变化时返回新数量和总大小并要求重新确认；实际删除仍逐项预检。
- 隐藏文件使目录删除安全失败；符号链接目录、Content 逃逸、跨设备移动和竞态删除均保持元数据与文件安全。
- 同一 UUID 的分析、原图读取和管理操作共享互斥锁，管理操作最多等待五秒；目录操作先取得所有后代锁。
- 任务页每两秒轮询并在组件卸载时清除计时器；扫描与缓存清理沿用单实例协调器。
- 小阈值轮转测试生成多个日志文件，断言总数不超过十个、级别筛选正确、归档内每个文件逐字节完整。
- 真实生产 Docker 中的 Chromium 管理旅程覆盖批量部分冲突、跨页快照删除、非空目录拒绝、任务轮询、日志筛选/下载和设置入口。

## 验收结果（2026-07-17）

- `scripts/acceptance/stage-05.sh`：通过。
- 阶段脚本内 Server 专项：24 passed（阶段 5 管理、日志和图片互斥相关测试）。
- 全量 Server 回归：89 passed。
- Web 单元测试、`vue-tsc --noEmit`、Vite 生产构建：通过。
- 阶段 4 Chromium 回归：8 passed。
- 阶段 5 生产 Docker Chromium 管理闭环：1 passed。
- OpenAPI `--check`：无漂移。
- 验收后：无运行中的 `dokura-stage-05` 容器，无残留 `dokura-stage-05.*` 临时目录。

## 已知边界

- 首版仍不提供批量重命名、递归目录删除、上传或跨文件系统复制后删除；这些操作按 PRD 明确拒绝。
- 日志归档保证每个已包含文件内容完整，不承诺多个日志文件之间的事务快照。
